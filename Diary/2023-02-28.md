Continuing work on removing -this-unit-id ghc by figuring out what the wired-in packages are based on the -package-id flags passed when compiling the module (we have to identify the wired-in packages in the list of -package-id flags)